<Window x:Class="FocusTimer.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowState="Normal">

    <Grid>
        <!-- Full-screen semi-transparent black background to cover the work area (excludes taskbar) -->
        <!-- Make it non-hit-testable so clicks pass through to underlying apps -->
        <Rectangle x:Name="BackgroundRect" Fill="#A0000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" IsHitTestVisible="False"/>

        <!-- Centered timer text (also non-hit-testable so clicks pass through) -->
        <TextBlock x:Name="TimerText"
                   Text="00:00"
                   Foreground="#AFFFFFFF"
                   FontSize="100"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   IsHitTestVisible="False"/>
    </Grid>
</Window>
